import DisplayCard from "./Pages/DisplayCard"

import styled from "styled-components";

const App = () =>{
    return(
        <AppContainer>
            <DisplayCard display = "displayOne"/>
        </AppContainer>
    );
};

export default App;

const AppContainer = styled.div`
    
`
